{
  "indexes": [
    {
      "collectionGroup": "rolex_tickets",
      "queryScope": "COLLECTION",
      "fields": [
        {"fieldPath": "status", "order": "ASCENDING"},
        {"fieldPath": "timestamp", "order": "ASCENDING"}
      ],
      "description": "Legacy: Cleanup of old 1-per-document reserved tickets (status == reserved AND timestamp < X)"
    },
    {
      "collectionGroup": "rolex_entries",
      "queryScope": "COLLECTION",
      "fields": [
        {"fieldPath": "status", "order": "ASCENDING"},
        {"fieldPath": "timestamp", "order": "DESCENDING"}
      ],
      "description": "NEW: Required for General/Super Admin Rolex Dashboard ('status' == 'paid' ORDER BY 'timestamp' DESC)"
    },
    {
      "collectionGroup": "rolex_entries",
      "queryScope": "COLLECTION",
      "fields": [
        {"fieldPath": "referrerUid", "order": "ASCENDING"},
        {"fieldPath": "status", "order": "ASCENDING"},
        {"fieldPath": "timestamp", "order": "DESCENDING"}
      ],
      "description": "NEW: Required for Referrer Rolex Dashboard (filter by 'referrerUid', status, and ORDER BY 'timestamp' DESC)"
    },
    {
      "collectionGroup": "splitThePotTickets",
      "queryScope": "COLLECTION",
      "fields": [
        {"fieldPath": "referrerUid", "order": "ASCENDING"},
        {"fieldPath": "timestamp", "order": "DESCENDING"}
      ],
      "description": "Required for Referrer Raffle Dashboard (filter by 'referrerUid' and ORDER BY 'timestamp' DESC)"
    },
    {
      "collectionGroup": "stripe_donation_payment_intents",
      "queryScope": "COLLECTION",
      "fields": [
        {"fieldPath": "status", "order": "ASCENDING"},
        {"fieldPath": "createdAt", "order": "DESCENDING"}
      ],
      "description": "Required for Super Admin Donations Dashboard (filter by 'status' and ORDER BY 'createdAt' DESC)"
    }
  ],
  "fieldOverrides": []
}
